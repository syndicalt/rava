[package]
name = "rava"
version = "0.1.0"
description = "CLI and preview verifier service for the Rava action-native authorization protocol"
edition.workspace = true
license.workspace = true
repository.workspace = true

[[bin]]
name = "rava"
path = "src/main.rs"

[dependencies]
clap = { version = "4.5", features = ["derive"] }
libc = "0.2"
rava-core = { version = "0.1.0", path = "../rava-core" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
time = { version = "0.3", features = ["formatting", "parsing", "serde"] }

[dev-dependencies]
assert_cmd = "2"
predicates = "3"

[lints]
workspace = true
